# Scaffolding generated by Backbone v.3.1.11

module Backbone
  class DashboardController < Backbone::BackboneController
  
    ## optional filters for defining usage according to Backbone::Users access_levels
    # before_filter :needs_admin, :except => [:action1, :action2]
    # before_filter :needs_admin_or_current_user, :only => [:action1, :action2]
  
    def index
    end

  end
end