<h1>Listing groups</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_bolt_group_path %>
